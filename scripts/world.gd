extends Node

@onready var camera = $Camera3D

# Called when the node enters the scene tree for the first time.
func _ready():
	#var camera_control_script = preload("res://scripts/cameraMovement.gd")
	#var camera_control_instance = camera_control_script.new()
	#camera.add_child(camera_control_instance)
	pass	
